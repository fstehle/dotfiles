# Install homebew cask applications at /Applications folder
export HOMEBREW_CASK_OPTS="--appdir=/Applications --fontdir=/Library/Fonts"

# Add homebrew to path
eval "$(/opt/homebrew/bin/brew shellenv)"
